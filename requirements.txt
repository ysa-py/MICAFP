# TorShield-IR Python requirements
# Install: pip install -r requirements.txt

# ── Core HTTP + HTML parsing ──────────────────────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── Async HTTP (core/ pipeline) ───────────────────────────────────────────────
aiohttp>=3.9.0

# ── ML blocking predictor (FEATURE 1) ────────────────────────────────────────
scikit-learn>=1.4.0
numpy>=1.26.0

# ── QUIC / Hysteria2 / next-gen protocol detection ───────────────────────────
# aioquic is the reference QUIC library for Python probing
aioquic>=1.0.0

# ── TLS fingerprinting (JA3 computation + ESNI/ECH awareness) ────────────────
cryptography>=42.0.0

# ── Post-quantum key-exchange awareness (Kyber / ML-KEM draft) ───────────────
# Used by quantum_safe.py to detect and score PQ-enabled bridges
pycryptodome>=3.20.0

# ── Terminal output (optional rich logging) ──────────────────────────────────
rich>=13.0.0

# ── YAML parsing (workflow validation in tests) ──────────────────────────────
PyYAML>=6.0.1

# ── Static analysis (CI quality gates) ──────────────────────────────────────
ruff>=0.4.0
mypy>=1.9.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0

# ── Advanced features: ECH / NIN bypass / anti-AI DPI ───────────────────────
# dnspython for DNS-over-HTTPS queries (ECH record lookup)
# ── Advanced features: ECH / NIN bypass / anti-AI DPI ───────────────────────
# dnspython for DNS-over-HTTPS queries (ECH record lookup)
dnspython>=2.6.0
# dpkt for raw packet inspection (JA3 fingerprinting)
dpkt>=1.9.8

# ── PDF report generation (scripts/generate_final_report.py) ────────────────
# REMEDIATION 2026-06-21: was imported unguarded with zero entry here —
# guaranteed ModuleNotFoundError on first run in any clean CI checkout.
reportlab>=4.0.0

# ── Ultra VIP Edition — additive dependencies (Phase 5) ─────────────────────
# nest-asyncio: supports asyncio.run inside already-running event loops (BUG-3 fix)
nest-asyncio>=1.6.0
# httpx: modern async HTTP client used by V4 anti-DPI module
httpx>=0.27.0
# tenacity: retry-with-backoff used by V2 self-healing engine
tenacity>=8.3.0
# structlog: structured logging used by V2 telemetry + self-heal
structlog>=24.1.0
# prometheus-client: metrics export for the VIP telemetry dashboard
prometheus-client>=0.20.0
# pytest-timeout: per-test timeout (used in pyproject.toml)
pytest-timeout>=2.3.0
